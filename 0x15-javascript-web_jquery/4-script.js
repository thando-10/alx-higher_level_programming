// Attaching a click event handler to DIV#toggle_header
$("DIV#toggle_header").click(function() {
    // Toggling the class 'red' and 'green' on the <header> element
    $("header").toggleClass("red green");
});

